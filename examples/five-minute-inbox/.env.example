# Server-only. Never expose these to the browser or commit real values.
# Project token or API key that can mint client tokens.
POLYMORFA_API_KEY=
# The session (WhatsApp number) the inbox reads and sends from.
POLYMORFA_SESSION=
# Signing secret of the webhook that points at /api/polymorfa/webhooks.
POLYMORFA_WEBHOOK_SECRET=
